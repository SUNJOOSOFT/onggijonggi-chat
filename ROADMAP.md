# 로드맵

방향을 공유하는 문서다 — 순서는 바뀔 수 있고 일정은 적지 않는다.

의견이나 필요한 것이 있으면 [이슈](../../issues/new/choose)로 알려주면 된다.

---

## 지금 — 공개본에 들어 있는 것

- **Keycloak(OIDC) 로그인** · 역할 기반 접근 제어 · 분당 요청 제한
- **스트리밍 채팅** — 응답이 생성되는 대로 출력, 대화 이력은 DB에 저장
- **실시간 협업 채팅** — 한 대화에 여러 명이 참여해 방 단위로 메시지를 주고받고, 누가
  접속해 있는지 보인다. **AI는 `@AI`로 부를 때만 답한다** — 사람끼리 주고받는 동안은
  끼어들지 않는다. 1:1 채팅도 같은 WS 경로로 통일됐고, 연결이 끊기면 자동으로 다시 붙는다
- **게이트웨이를 통한 모델 교체** — 설정만 바꾸면 되고 애플리케이션 코드는 그대로다.
  상용 API든 직접 띄운 로컬 모델이든 같은 방식으로 붙는다
- **`docker compose up` 한 번**으로 9개 서비스 전체 기동

브라우저는 **BFF 한 곳만** 호출한다. 게이트웨이와 모델 자격증명은 브라우저에 노출되지
않으며, 이 경계는 앞으로도 유지한다.

### 아직 안 되는 것

지금 버전에서 부딪히게 되는 것들이다. 고쳐야 할 것과 아직 만들지 않은 것을 함께 적는다.

- **문서 업로드·검색이 없다** — 모델이 아는 것만 답한다.
- **서버 한 대를 전제로 한다.** 접속자에게 메시지를 퍼뜨리는 구조가 인스턴스 하나를
  가정하고 있어서, 여러 대로 늘려 쓰려면 별도 설계가 필요하다. 그 시점이 오기 전까지는
  한 대로 운영하는 구성만 지원한다.
- **기본 구성은 평문 HTTP다** — HTTPS가 필요하면 [INSTALL_r-proxy.md](INSTALL_r-proxy.md)로
  리버스 프록시를 얹는다. 그 밖의 기본값 제약은
  [INSTALL.md — 알아둘 제약](INSTALL.md#알아둘-제약)에 정리돼 있다.

---

## 그 뒤 — 방향 (확정된 계획은 아니다)

무엇을 하려는지만 적는다. 어떻게 만들지는 정하지 않았고, 순서도 바뀔 수 있다.

- **소속에 따라 보이는 범위가 갈린다** — 조직이 여러 단계로 나뉘어 있어도 자기 자리에서
  볼 수 있는 것만 보이게 한다
- **문서를 올려두고 그 안에서 찾는다** — 모델이 아는 것에 더해, 올려둔 문서를 근거로
  답하고 어디서 가져왔는지 보여준다. 볼 수 없는 문서는 검색 결과에도 나오지 않는다
- **누가 무엇을 물었고 무엇이 막혔는지 남는다** — 감사 기록

---

이 문서에 없는 것을 필요로 한다면 [이슈](../../issues/new/choose)로 알려주면 된다.
쓰이는 곳을 알아야 순서를 정할 수 있다.
